import { FilterFieldsTypes } from './types/filter.types';

export const FILTER_FIELD_TYPES: FilterFieldsTypes = {
  clicks: 'between',
  host: 'in',
  date: 'between',
  status: 'in',
};
export enum test {
  CLICKS = 'between',
}
